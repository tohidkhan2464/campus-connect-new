import { toast } from "react-hot-toast";
import { searchEndpoints } from "../api";
import { apiConnector } from "../apiConnector";

const {
  POST_SEARCH_API,
  USER_SEARCH_API,
  NEWS_SEARCH_API,
  SEARCH_HOME_PAGE_API,
} = searchEndpoints;
